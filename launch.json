{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "run grunt",
            "protocol": "inspector",
            "program": "/Users/jmoses/n/bin/grunt",
            "args": ["cssmin"],
            "restart": true,
            "port": 5858,
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceRoot}/src/webapp-mms"
        },
        {   // works with npm tool chrome-debug (start it on a page then enable this)
            // note: doesn't work with sourcemaps on minified bundles' (missing sourceRoot perhaps?)
            "type": "chrome",
            "request": "attach",

            "name": "Attach to Chrome",
            "port": 9222,
            "webRoot": "${workspaceRoot}",
            "sourceMaps": true,
            "sourceMapPathOverrides": {
                "*": "${webRoot}/*"
            }
        }
    ]
}
